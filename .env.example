# Copy to .env for local dev. Do NOT commit .env — put the real password
# in Coolify's environment variables (with "Build Variable" enabled) instead.
#
# Leave blank to disable the gate for local dev.
VITE_APP_PASSWORD=
