# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Local env vars — never commit the real password
.env
.env.local
.env.*.local

# macOS signing credentials — local only, never push
.build-credentials

# macOS build scripts and docs — not needed by Coolify web deployment
build-and-sign.sh
macos-app-generate.md

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# macOS build artifacts
*.app
*.dmg
*.app.zip
build-log.txt
src-tauri/target/

# Allow the stable download in promo/ (served to users via Coolify)
!promo/DiscoCast-Visualizer.dmg

# Keep Tauri config, ignore only build output
# (src-tauri/tauri.conf.json, Cargo.toml, etc. should be committed)
